# Copy to `.env` in this folder and fill in values. Never commit `.env`.

# Atlas connection string (mongodb+srv://... from Atlas → Connect → Drivers)
MONGODB_URI=

# Default database name for this app
MONGODB_DB_NAME=

# Mistral API (receipt OCR / LLM — https://console.mistral.ai)
MISTRAL_API_KEY=
